class AgentContextFixture:
    # @agent-context invariant
    # Python hash comments need a manual folding range.
    # The assisted method must remain visible.
    def run(self) -> int:
        return 1
